<main role="main">
  <section class="jumbotron text-center" style="background-image: url('https://images.unsplash.com/photo-1468234847176-28606331216a?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1915&q=80');background-position: center center; background-size: cover;">
    <div class="container">
      <h1 class="jumbotron-heading text-light">Events</h1>
      <p class="lead text-light">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don’t simply skip over it entirely.</p>
      <%if user_signed_in?%>
      <p>
        <%=link_to "Créer un event", new_event_path, :class => "btn btn-primary my-2"%>
      </p>
      <%end%>
    </div>
  </section>

  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row">
        <% @events.each do |event| %>
        <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
            <div class="card-body">
              <h5 class="card-title">              
              <%=link_to "#{event.title}", event_path(event.id)%>
              </h5>
              <h6 class="card-subtitle mb-2 text-muted"><%= event.admin.first_name %> <%= event.admin.last_name %></h6>
              <p class="card-text"><%= event.description %></p>
                <div class="row">
                  <div class="col">
                    <%=event.attendances.count %> participant(s)
                  </div> 
                </div>
            </div>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</main>

<%# link_to 'Show', event %>
<%# link_to 'Edit', edit_event_path(event) %>
<%# link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %>